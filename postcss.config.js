export default {
  plugins: {
    // autoprefixer is not strictly needed if not using Tailwind,
    // but it's good practice to keep it for broader browser compatibility.
    autoprefixer: {},
  },
}
